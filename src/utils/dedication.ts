// ABOUTME: The name and message behind the heart button on the toolbar.
// ABOUTME: Kept out of i18n on purpose, since a dedication is not UI chrome to translate.

/** Shown as the button's tooltip, so hovering the heart says who it is for. */
export const DEDICATION_NAME = 'Andrea';

/** Revealed when the heart is clicked. Rendered as text, never as markup. */
export const DEDICATION_MESSAGE = 'Elsker deg pus <3';
